package kr.rmsxo.practice

import javax.inject.Inject

// Inject를 생성자에게 붙이면 바인딩을 하겠다 의존성을 추가한다.
// class MyName @Inject constructor() {
class MyName {

    override fun toString(): String {
        return "kgt"
    }
}